<%= form_for(@song_room) do |f| %>
  <% if @song_room.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@song_room.errors.count, "error") %> prohibited this song_room from being saved:</h2>

      <ul>
      <% @song_room.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
