<%= form_tag searches_path, method: :get, :class => "navbar-search" do %>
  <%= text_field_tag :query, params[:query], :placeholder => "Search Artists And Recording Rooms", :class => "search" %>
  <%= submit_tag "Submit", class: "dhide" %>
<% end %>
