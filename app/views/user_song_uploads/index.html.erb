<h1>Listing user_song_uploads</h1>

<table>
  <tr>
    <th>User</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @user_song_uploads.each do |user_song_upload| %>
  <tr>
    <td><%= user_song_upload.user_id %></td>
    <td><%= link_to 'Show', user_song_upload %></td>
    <td><%= link_to 'Edit', edit_user_song_upload_path(user_song_upload) %></td>
    <td><%= link_to 'Destroy', user_song_upload, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New User song upload', new_user_song_upload_path %>
