<%= form_for(@user_song_upload) do |f| %>
  <% if @user_song_upload.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_song_upload.errors.count, "error") %> prohibited this user_song_upload from being saved:</h2>

      <ul>
      <% @user_song_upload.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.text_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
